{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
    <main class="section">
        <div class="container">
            <h1 class="title">index</h1>
            <a href="{{ path('app_register') }}" class="button is-primary">Register</a>
            <a href="{{ path('app_login') }}" class="button is-primary">Log in</a>
            <a href="{{ path('app_logout') }}" class="button is-primary">Log out</a>
        </div>
    </main>
{% endblock %}
